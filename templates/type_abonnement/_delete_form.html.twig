<form method="post" action="{{ path('type_abonnement_delete', {'id': type_abonnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_abonnement.id) }}">
    <button class="btn">Delete</button>
</form>
